<Window x:Class="ZDesk.Windows.MemoDeleteConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="删除便笺" Width="430" Height="220" MinWidth="380" MinHeight="200"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True" Background="#FF1E1F26" Foreground="#FFF3F4F8"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="5" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#FFEFF2F8" />
            <Setter Property="Background" Value="#FF20232C" />
            <Setter Property="BorderBrush" Value="#FF40434F" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="13,7" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="#FF303746" />
                                <Setter TargetName="Surface" Property="BorderBrush" Value="#FF596071" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="#FF3A66B6" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="#FF6F9CDD" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.42" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Margin="1" Background="#FF202129" BorderBrush="#FF40434F" BorderThickness="1" CornerRadius="7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#FF171820" BorderBrush="#FF353741" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="14,0,8,0">
                    <TextBlock Text="删除便笺" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button Content="×" Width="30" Height="28" Padding="0" Margin="0" HorizontalAlignment="Right"
                            Click="Cancel_Click" ToolTip="取消" />
                </Grid>
            </Border>
            <StackPanel Grid.Row="1" Margin="18,16,18,10">
                <TextBlock Text="确定永久删除此便笺吗？" FontSize="15" FontWeight="SemiBold" />
                <TextBlock x:Name="MessageText" Margin="0,8,0,0" Foreground="#FFB5B6C1" TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,18,0">
                <Button Content="取消" IsCancel="True" Click="Cancel_Click" />
                <Button Content="永久删除" Margin="0" IsDefault="True" Background="#FF8F3D4C" BorderBrush="#FFC16D7A"
                        Click="Confirm_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
