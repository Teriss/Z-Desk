# Z-Desk 0.1.2 发布说明

- 修复布局元素跨布局移动时整组图标闪烁的问题，未变化的元素保留原有对象，仅更新实际变化项。
- 优化映射文件夹和桌面文件变更的增量刷新，减少 Shell 图标、元数据和 UI 重建开销。
- 修复重复快捷方式路径导致的布局状态异常和 `BSManager.lnk` 崩溃。
- 增加后台任务取消、快捷方式解析缓存和有界图标缓存，降低长期运行时的资源占用。
- 预热 Shell 原生右键菜单，改善重启后第一次打开菜单的响应速度。
- 合并重复的文件系统通知和布局保存请求，减少高频操作下的后台负载。
